({
    mostraCarregamento : function(component) {
        component.set("v.carregamento", false);
    },

    escondeCarregamento : function(component) {
        component.set("v.carregamento", true);
    }
})